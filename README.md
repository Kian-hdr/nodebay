# Nodebay update feeds

The `stable` directory is reserved for verified production releases. The `testing` directory is isolated prerelease verification infrastructure. It can deliberately contain invalid signatures or incomplete download fixtures during acceptance testing and must not be used by production builds. Tests never replace a published production archive.
